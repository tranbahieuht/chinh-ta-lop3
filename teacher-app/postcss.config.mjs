// Teacher app uses plain global CSS. Keep this local config so a deployment
// rooted at teacher-app never inherits the parent student app's Tailwind plugin.
const config = {
  plugins: {},
};

export default config;
